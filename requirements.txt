# Core (CPU-testable) — laptop/CI에서 검증 가능한 최소 스택
numpy
pillow
pytest
torch
transformers>=4.57.0     # qwen3_vl 지원 버전
qwen-vl-utils>=0.0.14    # Qwen3-VL-Reranker 공식 멀티모달 런타임 요구사항
accelerate
peft
datasets
huggingface_hub
safetensors
kagglehub                # data_download.py (fresh clone에서 data/ 자동 확보)
